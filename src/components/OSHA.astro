---
import type { Prop as CardProps } from "./carouselCustom/CustomCard.astro";
import CustomCarousel from "./carouselCustom/CustomCarousel.astro";

interface Props {
  title: string;
  subTitle: string;
  items: CardProps[];
}

const { title, subTitle, items } = Astro.props;
---

<h1 class="text-4xl text-blue-footer font-semibold text-center">{title}</h1>
<br />
<p class="text-center text-xl text-main tracking-wider">
  {subTitle}
</p>
<br />
<CustomCarousel items={items} />
